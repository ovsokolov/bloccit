<h1><%= @topic.name %></h1>
<% if user_is_authorized_for_topic_action?("edit") %>
   <%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-success' %>
<% end %>
<% if user_is_authorized_for_topic_action?("delete") %>
   <%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this topic?' } %>
<% end %>

 <div class="row">
   <div class="col-md-8">
     <p class="lead"><%= @topic.description %></p>
     <h3 class="media-heading text-success">Posts</h3>
        <%= render @topic.posts %>
     <h3 class="media-heading text-info">Sponsored Posts</h3>
        <%= render @topic.sponsored_posts %>
   </div>
   <div class="col-md-4">
     <% if current_user %>
        <%= link_to "New Post", new_topic_post_path(@topic), class: 'btn btn-success' %>
        <%= link_to "New Sponsored Post", new_topic_sponsored_post_path(@topic), class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
